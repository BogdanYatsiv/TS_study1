let age: number = 20;

if (age < 50) {
    age += 50;
}

console.log(age);

let sales: number = 123_456_789; // it TS we can separate digits to make code more readable
let course: string = "Typescript";
let is_published: boolean = true;
let level;

